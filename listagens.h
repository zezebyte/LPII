#ifndef LISTAGENS_H_
#define LISTAGENS_H_
#include "types.h"

void ListarRoloCod(ApArmazem); //lista o conteudo de qualquer rolo
void ListarContPack(ApArmazem);//lista o conteudo de qualquer pack
void MetragemExpd(ApArmazem);  //calcula a metragem de uma expedicao
void QuantRolosPacksGuia(ApArmazem); //conta quantos packs e rolos tem uma determinada guia

#endif /* LISTAGENS_H_ */
